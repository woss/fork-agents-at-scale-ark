[project]
name = "ark-sdk-builder"
version = "0.1.63-rc.1"
requires-python = ">= 3.12"
dependencies = [
    "build",
    "pytest",
    "pyyaml",
    "kubernetes",
    "pydantic",
    "urllib3>=2.6.3",
]

[tool.setuptools]
py-modules = []
