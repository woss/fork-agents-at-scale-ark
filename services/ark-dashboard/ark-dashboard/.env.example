#The base url of the next js application
BASE_URL=http://localhost:3000
#The url we use for handling auth related calls
AUTH_URL=http://localhost:3000/api/auth
#Used to encrypt tokens, it’s mandatory to keep things secure
AUTH_SECRET=
#The issuer url from your oidc provider
OIDC_ISSUER_URL=
#The client id from your oidc provider
OIDC_CLIENT_ID=
#Optional client secret from your oidc provider
OIDC_CLIENT_SECRET=
#The name of your oidc provider, it is used when the signin option is displayed on the UI
OIDC_PROVIDER_NAME=
#A custom id for the oidc provider.
#Keep in mind, the callback url you configure in your oidc provider has to end with this id.
#E.G. If your OIDC_PROVIDER_ID=exampleid then callback url must be http://localhost:3000/api/auth/callback/exampleid
OIDC_PROVIDER_ID=
#Space-separated OAuth scopes requested from your oidc provider.
#Defaults to "openid email profile". Add "offline_access" to receive refresh tokens (required by some providers, e.g. Dex, to keep sessions alive).
#E.G. OIDC_SCOPES=openid email profile offline_access
OIDC_SCOPES=
#The JWT will expire after this amount of time.
#If the user is inactive for this long, they will be signed out.
#In seconds, defaults to 30mins
#E.G. 1800 (30 * 60) = 30mins
SESSION_MAX_AGE=
#Interval in milliseconds for periodic refresh of access and refresh tokens from your OIDC provider
#Defaults to 10mins
#E.G. 300000 (5 minutes) means tokens will be refreshed every 5 minutes
NEXT_PUBLIC_TOKEN_REFRESH_INTERVAL_MS=
#Inactivity Timeout in milliseconds
#Defaults to 30mins
#E.G. 300000 (5 minutes) means the user will be signed out automatically after 30 mins of inactivity
NEXT_PUBLIC_FALLBACK_INACTIVITY_TIMEOUT=
#The authentication mode. If you set it to "open", the application will not require you to sign in.
AUTH_MODE=sso